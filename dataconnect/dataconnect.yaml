specVersion: "v1"
serviceId: "lifematters"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "lifematters-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
